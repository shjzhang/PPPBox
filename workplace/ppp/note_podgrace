1.关于GRACE的天线相位中心改正。
未找到GRACE卫星的相位文件。故在podforgrace.cpp的数据处理process中，
将//     pList.push_back(corr);       // Add to processing list注释掉。
引起的结果：应该是解出来的dx，dy，dz中包含了。天线相位中心偏差PCV+PCO。

考虑到:
a.GRACE卫星的星固系指向和局部指北坐标系指向之间的差异小于1°（GRACE轨道倾角89°）
b.L1/L2载波的PCO偏差小于2cm，PCV的量级优于5cm。
c.采用PRD内插的轨道作为初值，如果做了精确的PCV+PCO改正,dx/dy/dz，就是定轨误差，白噪声
如果没做改正
猜测，将dx，dy，dz旋转到局部指北坐标系系，其结果就是星固系下的天线相位中心偏差PCO，为常数+白噪声。
（前提：采用PRD轨道作为初值。）
验证结果：如推论。结果文件。*。out 的8-10列为局部指北坐标系下的解。除开异常，基本保持为常数，且与星固系下的PCO一致。

参见gpstk——old里提供的pod.conf文件。
本目录下为pod_gpstkold.conf

# Antenna parameters
offsetL1,  dX dY dZ ( SRF ) = -0.0004 -0.0004 -0.4514, meters
offsetL2,  dX dY dZ ( SRF ) = -0.0004 -0.0004 -0.4756, meters
offsetARP, dX dY dZ ( SRF ) =  0.0000  0.0000  0.0000, meters  

2.关于GPS卫星钟差,GPSTK_old里采用的是igs的30s钟差.参见pod_gpstkold.conf

3。关于结果，计算用的o文件和prd轨道文件以及gpstk——old的结果和设置，无法上传。QQ离线传送。 

因为提供的观测数据是2011/10/7-9三天的结果。之前计算的是09一天的结果。单纯比较09一天的结果与gpstkold里计算的精度和异常基本一致。（考虑到，未改正PCV，周期性小项。）



